import React, { useState } from 'react';
import IconButton from '@material-ui/core/IconButton';

const CarDashbord = (props) => {
 
  return (
  <>
    <IconButton onClick={props.onChange}>             
      salam
    </IconButton>
  </>
  )

};

export default CarDashbord;
